// what-compiler/runtime — the runtime helpers the compiled output relies on.
// Re-exported from what-core so compiled What code has a typed runtime.

export { h, Fragment, mount, Island } from 'what-core';
