name: Release And Deploy

on:
  workflow_dispatch:
    inputs:
      publish_packages:
        description: Publish packages to npm
        required: true
        default: false
        type: boolean
      deploy_web:
        description: Deploy configured web surfaces to Vercel
        required: true
        default: false
        type: boolean
      deploy_targets:
        description: Comma-separated deploy targets (empty = defaults in script)
        required: false
        default: ""
        type: string
      npm_tag:
        description: npm dist-tag for publish (use backport for 0.6.x backports; latest requires versions above npm latest)
        required: false
        default: latest
        type: string
      dry_run:
        description: Run without publishing/deploying
        required: true
        default: true
        type: boolean
      allow_non_latest:
        description: Acknowledge intentional non-latest/backport npm publish (requires non-latest npm_tag; use with npm_tag=backport)
        required: true
        default: false
        type: boolean

concurrency:
  group: release-and-deploy-main
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Verify quality gates
        run: npm run -s release:verify

  publish:
    needs: verify
    if: ${{ inputs.publish_packages }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.dry_run && 'release-dry-run' || 'production-release' }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Validate npm release channel
        run: |
          # Backport publishes must use npm_tag=backport with allow_non_latest=true.
          # Normal latest releases stay protected: package versions must be above npm latest
          # and allow_non_latest must remain false.
          if [ "${{ inputs.npm_tag }}" != "latest" ] && [ "${{ inputs.allow_non_latest }}" != "true" ]; then
            echo "Non-latest npm_tag requires allow_non_latest=true." >&2
            exit 1
          fi
          if [ "${{ inputs.npm_tag }}" = "latest" ] && [ "${{ inputs.allow_non_latest }}" = "true" ]; then
            echo "allow_non_latest can only be used with a non-latest npm_tag." >&2
            exit 1
          fi
          if [ "${{ inputs.allow_non_latest }}" = "true" ] && [ "${{ inputs.npm_tag }}" != "backport" ]; then
            echo "Backport is the only allowed non-latest npm_tag for this branch." >&2
            exit 1
          fi

      - name: Publish npm packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          ARGS=()
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            ARGS+=(--dry-run)
          fi
          if [ -n "${{ inputs.npm_tag }}" ]; then
            ARGS+=(--tag "${{ inputs.npm_tag }}")
          fi
          if [ "${{ inputs.allow_non_latest }}" = "true" ]; then
            ARGS+=(--allow-non-latest)
          fi
          if [ "${{ inputs.dry_run }}" != "true" ]; then
            ARGS+=(--provenance)
          fi
          node scripts/publish-packages.mjs "${ARGS[@]}"

      - name: Verify npm registry packages
        if: ${{ !inputs.dry_run }}
        run: npm run -s registry:smoke

      - name: Upload registry smoke artifact
        if: ${{ !inputs.dry_run && always() }}
        uses: actions/upload-artifact@v4
        with:
          name: what-registry-smoke
          path: artifacts/registry-smoke.json
          if-no-files-found: error

  deploy:
    needs: [verify, publish]
    if: ${{ always() && inputs.deploy_web && needs.verify.result == 'success' && (!inputs.publish_packages || needs.publish.result == 'success') }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.dry_run && 'deploy-dry-run' || 'production-deploy' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Deploy Vercel targets
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          ARGS=()
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            ARGS+=(--dry-run)
          fi
          if [ -n "${{ inputs.deploy_targets }}" ]; then
            ARGS+=(--targets "${{ inputs.deploy_targets }}")
          fi
          node scripts/deploy-vercel.mjs "${ARGS[@]}"
