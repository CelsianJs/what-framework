{
  "name": "what-krausest-benchmark",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "js-framework-benchmark (krausest) style keyed benchmark: what vs react vs solid vs vanilla. Deps are isolated here — install with `npm install` IN THIS DIRECTORY (not at the repo root).",
  "scripts": {
    "build": "node build-all.mjs",
    "bench": "node bench.mjs",
    "bench:quick": "node bench.mjs --quick"
  },
  "devDependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "solid-js": "^1.9.9",
    "vite": "^7.3.1",
    "vite-plugin-solid": "^2.11.9"
  }
}
