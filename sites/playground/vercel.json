{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "i=0;until npm install 2>/tmp/e;do cat /tmp/e;grep -q ETARGET /tmp/e||exit 1;i=$((i+1));[ $i -ge 10 ]&&exit 1;echo \"[what] pinned what-* version not on npm yet; retry $i/10 in 20s\";sleep 20;done"
}
