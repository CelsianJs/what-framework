{
  "name": "what-docs-site",
  "private": true,
  "version": "0.12.3",
  "type": "module",
  "description": "whatfw.com, built with What Framework (SSG via what-server renderToString)",
  "scripts": {
    "check": "node scripts/check-api-refs.mjs",
    "prebuild": "npm run -s check",
    "build": "node build.mjs",
    "preview": "node build.mjs && npx --yes serve dist -l 4178"
  },
  "dependencies": {
    "what-framework": "0.12.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
